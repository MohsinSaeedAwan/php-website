pipeline {
    
    stages {
        stage('Build') {
            steps {
               sh 'cd /usr/share/nginx/html/ && git stash && git pull'
            }
        }
     stage('Deliver') {
            steps {
                sh 'echo "Changes Deployed"'
            }
        }
    }
}
